apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-db
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.db.labels.app }}

spec:
  serviceName: "{{ .Release.Name }}-db"
  replicas: 1  # Postgres StatefulSet mein always 1 rakhte hain (no multi-master)

  selector:
    matchLabels:
      app: {{ .Values.db.selectorLabels.app }}

  template:
    metadata:
      labels:
        app: {{ .Values.db.labels.app }}

    spec:
      containers:
        - name: {{ .Chart.Name }}-db
          image: "{{ .Values.db.image.repository }}:{{ .Values.db.image.tag }}"
          imagePullPolicy: {{ .Values.db.image.pullPolicy }}

          ports:
            - containerPort: {{ .Values.db.service.port }}
              protocol: TCP

          # ✅ Secret se credentials — plain YAML mein password nahi likhte
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-db-secret
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-db-secret
                  key: postgres-password

          # ✅ requests aur limits dono
          resources:
            requests:
              cpu: {{ .Values.db.resources.requests.cpu }}
              memory: {{ .Values.db.resources.requests.memory }}
            limits:
              cpu: {{ .Values.db.resources.limits.cpu }}
              memory: {{ .Values.db.resources.limits.memory }}

          # ✅ PVC se data mount
          volumeMounts:
            - name: db-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres  # subPath zaruri hai Postgres ke liye

          # ✅ readinessProbe — pg_isready check karta hai DB ready hai ya nahi
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3

          # ✅ livenessProbe — agar DB hang ho jaye toh restart
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 5

  # ✅ PVC template — pod ke saath automatically create hota hai
  volumeClaimTemplates:
    - metadata:
        name: db-storage
      spec:
        accessModes:
          - {{ .Values.db.persistence.accessMode }}
        resources:
          requests:
            storage: {{ .Values.db.persistence.size }}
        {{- if .Values.db.persistence.storageClass }}
        storageClassName: {{ .Values.db.persistence.storageClass }}
        {{- end }}